;(function (w){
    'use strict';

    w.Global = function () {
        var config = {
            server: 'http://localhost:3000/'
        };

        return {
            config: config
        };
    };
}(window));
